# 手動確認チェックリスト

このプロジェクトに自動テストは存在しない。演出込みのブラウザ挙動を目視で確認する以外に検証手段がないため、コードを変更した際は関係する項目をブラウザで実際に操作して確認すること。

`state` / `imgs` を直接触るような変更(座標計算、演出タイミング、スプライト切り替えなど)は、見た目に出るまで気づきにくいので特に念入りに確認する。

## 使い方

- 変更した機能グループ(`ARCHITECTURE.md`参照)に対応する項目だけ確認すればよい。全項目を毎回確認する必要はない。
- `index.html` をブラウザで直接開いて確認する。ブラウザのDevToolsコンソールにエラーが出ていないかも合わせて確認する。

## シーン遷移

- [ ] ロゴシーンがフェードイン→2秒表示→フェードアウトの後、自動でプロローグへ進む
- [ ] ロゴシーンを画面タップでスキップできる
- [ ] プロローグが4画面再生され、SKIPボタンで中断できる
- [ ] プロローグ終了後、自動でタイトルへ進む
- [ ] タイトルでNEW GAMEを押すとストーリーシーン(3画面)を経てデッキ編成に進む
- [ ] タイトルでTRAINING MODEを押すとストーリーシーンを経ずデッキ編成に進む
- [ ] セーブデータがある状態でタイトルを開くとCONTINUEボタンが表示される
- [ ] CONTINUEを押すとストーリーシーンを再生せず直接デッキ編成に進む

## デッキ編成

- [ ] PUNCH/UPPER/GUARDの±ボタンで枚数が変わる
- [ ] 合計が21枚以外のときFIGHTボタンが押せない
- [ ] 合計が21枚のときFIGHTボタンが押せる
- [ ] 0枚未満、または合計21枚を超える増減ができない

## バトル開始演出

- [ ] 暗転→キャラのフェードイン→背景の円形拡大→「BATTLE START」のディゾルブ演出が一通り再生される
- [ ] 手札が裏向きで配られ、左から順にめくられる
- [ ] 演出完了まで手札タップ・GO!ボタンが反応しない
- [ ] 演出完了後、操作可能になる

## バトル基本ループ(STORY MODE)

- [ ] カードをタップすると場(5枠)に順番に置かれる
- [ ] CANCELで場のカードが手札に戻る
- [ ] 場が空のときGO!/CANCELが押せない
- [ ] GO!を押すと敵の手が生成され、攻防が1件ずつ解決される
- [ ] 敵の手は攻防の直前に1枚ずつ公開される(先読みできない)
- [ ] 3すくみ判定通りに勝敗が決まる(PUNCH>UPPER>GUARD>PUNCH)
- [ ] 同じ手同士は相討ちになり、双方が微ダメージを受ける
- [ ] UPPER勝利時、攻撃側の次の手がPUNCHなら空中コンボが継続する(最大3発、3発目はメテオ)
- [ ] GUARD勝利時、負けた側がしびれ、次の攻防で無条件敗北することがある(発生確率のため複数回確認)
- [ ] しびれがターンをまたいで持ち越されない
- [ ] ターン終了時、双方がホームポジションへ帰還する
- [ ] 使用したカードが捨札に送られ、使った枚数分だけ山札から補充される
- [ ] 手札・山札が共に尽きたときのみ、DECKのリフレッシュ演出(点滅→カウントアップ)が発生する
- [ ] HPが0になった側でK.O./YOU WINの決着演出が正しく再生される

## TRAINING MODE

- [ ] 敵の手がカードを選ぶ前から5枠すべて公開されている
- [ ] 敵の技がターンごとにPUNCH→UPPER→GUARDの順で切り替わる
- [ ] ターン終了時に双方のHPが全回復する
- [ ] HPが0になってもK.O./YOU WIN判定が発生しない

## 決着オーバーレイ

- [ ] K.O.時はCONTINUEボタンと「タイトルへ戻る」ボタンの両方が表示される
- [ ] YOU WIN時は「タイトルへ戻る」ボタンのみ表示される
- [ ] CONTINUEを押すと直前のデッキ編成画面に戻る
- [ ] 「タイトルへ戻る」を押すとロゴシーンから再生される

## HOW TO / OPTION

- [ ] どのシーンからでもHOW TOボタンでポップアップが開く
- [ ] タイトル画面から開いたOPTIONにはRETRY/RETURN TO TITLEボタンが表示されない
- [ ] バトル画面から開いたOPTIONにはRETRY/RETURN TO TITLEボタンが表示される
- [ ] SOUND ON/OFFの選択がリロード後も保持される
- [ ] 進行状況リセットで確認ダイアログが出て、OKで`storyEnemyIndex`が0に戻る
- [ ] 図鑑ボタンで「準備中です」のアラートが出る(既知の未実装挙動)

## セーブ/ロード(localStorage)

- [ ] デッキ編成・サウンド設定・STORY MODEの進行状況がリロード後も復元される
- [ ] localStorageを手動で削除した状態でも起動時にエラーにならない

## アセットのフォールバック

- [ ] `assets/`配下の任意アセット(敵グラフィック/背景/ロゴ/カード画像/プロローグ・ストーリー画像)を意図的に未配置の状態にしても、`ASSETS.md`記載のフォールバック表示になりエラーで止まらない
- [ ] 必須アセット(`assets/images/characters/`配下)が読み込めない場合、コンソールにエラーが出力される(第12条)
