# AI作業ガイド

このプロジェクトは複数のAI(および人間)が入れ替わりで作業する前提で運用されている。
作業を始める前に、必ずこのファイルを読むこと。

## 作業開始前に必ず確認すること

1. 実装・仕様に関わる作業をする場合は [`CONSTITUTION.md`](./CONSTITUTION.md) を確認する。特にバトルロジックや演出を変更する場合は必読。
2. 既存の実装状況は [`IMPLEMENTATION.md`](./IMPLEMENTATION.md)、未実装事項は [`TODO.md`](./TODO.md) を確認する。
3. アセット(画像など)を追加・変更する場合は [`ASSETS.md`](./ASSETS.md) の命名規則を確認する。
4. コードの修正箇所を探す場合は、`js/script.js`を全文検索・全読みする前に [`ARCHITECTURE.md`](./ARCHITECTURE.md) の関数マップで見当をつける。
5. 実装済み機能が期待通り動かない場合は [`KNOWN_ISSUES.md`](./KNOWN_ISSUES.md) を確認し、既知の不具合でなければ同フォーマットで追記する。

無関係な作業(例: ストーリーテキストのみの編集)であれば、上記すべてを毎回読み込む必要はない。作業内容に直接関係するドキュメントのみを読めばよい。

## 絶対に守ること

- **憲法二十六条(`CONSTITUTION.md` / `js/script.js` 冒頭コメント)を省略・簡略化・要約しない。** 全文をそのまま維持すること(第25条)。片方を変更したらもう片方にも同じ変更を反映する。
- **未確認の仕様を推測で書き足さない。** コード・コメント・ドキュメントいずれにおいても、実装されていない挙動をあたかも仕様であるかのように記述しない。分からない場合は、その場で作り込まずに利用者へ質問するか、`TODO.md`または`KNOWN_ISSUES.md`に「不明点」として明記する。
- **情報源が食い違う場合は `CONSTITUTION.md` を最優先の正とする。** 次に実コード(`js/script.js`)、その次に他のMarkdown(`IMPLEMENTATION.md`等)の順で判断する。別のAI(ChatGPT/Codex等)からの過去の提案や会話履歴は、これらと矛盾する場合は採用しない。
- **`state` / `imgs` オブジェクトを再定義・再初期化しない**(第13条)。既存のプロパティを書き換える形で扱うこと。
- **アセットの参照キーとファイルの読み込みパスを混同しない。** `imgs` 辞書のキーは常に短いファイル名(例: `'player.PNG'`)を使う。フォルダを跨いだ読み込みパス(例: `'assets/images/characters/player.PNG'`)は `Image.src` や CSS の `url()` にのみ使用し、`imgs[...]` のキーには使わない。これを混同すると画像が読み込めているのに参照だけ失敗し、フォールバック(赤緑の四角/文字表示)に静かに切り替わるバグが発生する。
- **アセットのファイル名規則**: 拡張子は常に `.PNG`(大文字)、ファイル名部は小文字(第22条)。
- **`js/script.js` は物理的に1ファイルのまま、`ARCHITECTURE.md` の8モジュール構成に沿って論理分割されている。** 新しい関数を追加する場合は、対応するモジュールのセクション見出しコメント(`// ====...` で囲まれた節)の中に配置し、`ARCHITECTURE.md` の関数一覧にも追記すること。物理ファイル分割(`battle.js`のような複数ファイル化)は、ビルドツール導入等の前提が整うまで行わない。
- **コード修正時は、差分(パッチ形式)ではなく、変更があったファイルのみ全文で出力する。** 利用者はGitHub上でファイルの中身をまるごと貼り替える運用のため、差分適用は行わない。変更のないファイル(`index.html` / `css/style.css` / `js/script.js` のうち触っていないもの)は出力しない。

## 変更のスコープに関するルール

- **1回の変更は1つの目的に限定する。** 依頼された内容と無関係な修正を同じ変更にまとめない。
- **バグ修正と仕様変更を同時に行わない。** 「直したらついでに仕様も変えておいた」は禁止。仕様変更が必要だと判断した場合は、その旨を伝えて別の作業として提案する。
- **動作変更を伴うリファクタリングは、明示的に依頼された時のみ行う。** 「ついでに整理しておきました」は禁止(既存の「明示的な指示がない限り行わないこと」と合わせて徹底する)。
- 変更が複数ファイル・複数目的にまたがりそうな場合は、着手前に分割案を提示し、利用者の確認を取ってから進める。

## コーディング規約(既存コードとの一貫性のため)

- コメントは日本語で記述する(既存コードに合わせる)。
- キャラクターの状態・演出はcanvas上へ`drawImage`で描画する(DOM要素での表現ではない)。
- 演出処理は `async/await` を用いた順次シーケンスとして書く(既存の `runNormalHit` / `runUpperCombo` / `runMeteor` 等のパターンに合わせる)。
- 設定値・状態は `DB`(定数)・`state`(可変状態)のような中央集権的なオブジェクトにまとめる。個々の関数内にマジックナンバーを散らさない。
- 任意アセット(存在しなくてもエラーにしないアセット)を追加する場合は、既存の `onerror` フォールバックパターンに倣う。

## コードを変更した場合の更新義務

コード(`index.html` / `css/style.css` / `js/script.js`)や仕様に変更を加えた場合、以下を作業の一部として更新すること。

- 新しく実装した内容 → `IMPLEMENTATION.md` に追記し、`TODO.md` から該当項目があれば削除する。
- 対応しきれなかった/後回しにした内容 → `TODO.md` に追記する。
- 関数構成やstateプロパティに変更があった場合 → `ARCHITECTURE.md` の該当箇所を更新する。
- 発見したが未対応の不具合 → `KNOWN_ISSUES.md` に追記する。対応した場合は「対応済み」に更新する。
- 変更内容 → `CHANGELOG.md` に日付・変更概要を追記する。
- 変更後は、`TESTING.md`のうち関連する項目を利用者に提示する。AI自身はブラウザを実際に操作して動作確認することができないため、「どの項目を確認すればよいか」を明示し、実際の確認は利用者に委ねる。

## 明示的な指示がない限り行わないこと

- コードの共通化・リファクタリング(依頼された作業の副産物として不要な変更を混ぜない)。
- 憲法二十六条の解釈を変えるような仕様変更。
- ドキュメントへのコードの丸ごと貼り付け(「コードは出力しない」と指示された場合は、識別子名などの最小限の参照に留める)。
