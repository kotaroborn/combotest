# コマンドバトル 実装状況ドキュメント

現時点で実装済みの内容のみを記載する。未実装・予定の内容は [`TODO.md`](./TODO.md) を参照。仕様の根拠は [`CONSTITUTION.md`](./CONSTITUTION.md) および実コード。

対象プロジェクト構成:

```
command-battle/
├── index.html
├── css/style.css
├── js/script.js
└── assets/
    ├── images/(characters, characters_enemy, backgrounds, logo, cards, cutscenes/opening, cutscenes/story)
    ├── audio/(bgm, se)
    └── fonts/
```

---

## 1. 画面遷移(実装済み)

以下の順で画面が遷移する。

1. **ロゴシーン**: フェードイン→2秒表示→フェードアウトの後、自動でプロローグへ遷移する。画面タップでスキップ可能。
2. **プロローグシーン**: 画像+1文字ずつ表示されるテキストを4画面分再生する。SKIPボタンあり。4画面終了後は自動でタイトルへ遷移する。
3. **タイトルシーン**: NEW GAME / TRAINING MODE / OPTION のボタンを表示する。セーブデータが存在する場合のみ CONTINUE ボタンも表示される。
4. **ストーリーシーン**(STORY MODEでNEW GAMEを選んだ場合のみ): プロローグと同じ仕組みで画像+テキストを3画面再生する。CONTINUE/RETRYでは再生されない。SKIPボタンあり。3画面終了後は自動でデッキ編成へ遷移する。
5. **デッキ編成シーン**: PUNCH/UPPER/GUARDの枚数を調整し、合計21枚になったときのみFIGHTボタンが有効になる。
6. **バトルシーン**: バトル開始演出→手札配布演出の後、操作可能になる。
7. **決着オーバーレイ**: K.O.またはYOU WINを表示する。K.O.時のみCONTINUEボタンが表示され、両方のケースで「タイトルへ戻る」ボタンが表示される。

---

## 2. ゲームモード(実装済み)

- **STORY MODE**: デッキ編成前にストーリーシーンを挟む。敵はプリセット(`ENEMY_PRESETS`)に基づいて生成される。
- **TRAINING MODE**: ストーリーシーンを挟まずデッキ編成へ進む。敵は毎ターン PUNCH→UPPER→GUARD の順で技を出し、カードを選ぶ前から5枠すべてが公開される。ターン終了ごとに双方のHPが全回復し、K.O./YOU WIN判定を行わない。

---

## 3. バトルシステム(実装済み)

- **5枠手札システム**: 場に最大5枚まで手札からカードを出せる。使用した枚数分だけ山札から補充する(山札の残数を超えては補充しない)。
- **デッキ21枚固定**: デッキ編成画面でPUNCH/UPPER/GUARDの内訳を自由に配分できる(合計21枚固定)。
- **敵コマンド非公開**: STORY MODEでは、プレイヤーが出した枚数だけ敵の手をその場でランダム生成し、各攻防の直前に1枚ずつ公開する。
- **3すくみ判定**: PUNCH>UPPER、UPPER>GUARD、GUARD>PUNCH。同じ手同士は相討ちになる。
- **空中コンボ**: UPPERで勝利した場合に発動。攻撃側の次の入力がPUNCHなら継続し、最大3発まで空中パンチを受け付ける。3発目は自動的にメテオに変換される。
- **しびれ**: GUARDが勝った場合、負けた側はしびれ状態になり、次の攻防で1/2の確率で3すくみ判定なしの無条件敗北になる。ターンをまたいで持ち越さない。
- **ダメージ数値**(コード内定数 `DB.DMG` より):
  - PUNCH勝利: 10(地上での連続ヒットごとに+5、途切れるとリセット)
  - UPPER初撃: 5
  - 空中パンチ連続ヒット: 2発目以降+5
  - メテオ: 50
  - 相討ち: 3
  - GUARD成功時の反撃: 1
- **手札・山札の補充/リシャッフル**: 手札と山札が共に尽きた場合のみ、捨札を山札へリシャッフルする演出(DECK表示の点滅→カウントアップ)が発生する。配分比率は変化しない。

---

## 4. 演出(実装済み)

- 攻防のたびに双方が残像付きで画面中央へ踏み込み、技を出し合う。
- 攻防後は初期位置(ホームポジション)へは戻らず、軽く距離を取るだけ。ホームポジションへ戻るのはターン完了時のみ。
- ダメージ発生時、HPバーの実数値は即座に減少し、黄色いバーは0.6秒遅れて追従する。
- ダメージを受けた側は一瞬点滅する。3すくみに負けた側のみ振動する。
- ターン数は画面上部中央に2行(「TURN」/数字)で表示される。
- 敵の画像は常に左右反転して描画される。
- 待機中は player.PNG / player2.PNG を一定間隔で交互に表示する呼吸演出がある。
- 空中コンボ中の連続パンチは punch.PNG と punch2.PNG を交互に使用する。
- 体力が0になる最後の一撃を受けた場合、通常のターン完了処理の代わりに専用の決着演出(点滅→バウンドしながら初期位置へ戻る→倒れる→結果表示)が実行される。
- バトル開始時のみ、暗転→キャラのフェードイン→背景の円形拡大表示→「BATTLE START」のディゾルブ演出→手札が裏向きで配られてから左から順にめくられる、という一連の演出が行われる。以降の手札補充ではこの演出は行われない。

---

## 5. 画像アセット管理(実装済み)

- 全アセットのファイル名は小文字、拡張子は常に`.PNG`(大文字)。
- 味方キャラクター画像(`DB.ASSETS`内の12種)は必須アセットとして扱われ、読み込みエラー時はコンソールにエラーを出力する。
- 以下は任意アセットとして扱われ、存在しなくてもエラーにならずフォールバック表示に切り替わる(実装済みのフォールバック挙動):
  - 敵専用グラフィック(`enemy_〇〇.PNG`): 未配置ならプレイヤー画像を使用する。
  - 背景(`bg.PNG`): 未配置なら水色の塗りつぶしになる。
  - ロゴ(`logo.PNG`)・タイトルロゴ(`title_logo.PNG`): 未配置ならテキスト表示になる。
  - カード表面(`card_P/U/G.PNG`)・裏面(`card_back.PNG`): 未配置ならP/U/Gの文字表示、または縞模様になる。
  - プロローグ画像(`op_1〜4.PNG`)・ストーリー画像(`story_1〜3.PNG`): 未配置ならプレースホルダー表示になる。

---

## 6. セーブ/ロード(実装済み)

localStorage(キー: `commandbattle_save_v1`)に以下の値が保存され、次回起動時に自動で復元される。

- `storyEnemyIndex`(STORY MODEの進行状況)
- `deckCounts`(デッキ編成の内訳)
- `soundOn`(サウンド設定値。値の保存のみで、音の再生自体は未実装)
- `unlockedItems`(取得済みアイテムの配列。読み込み処理はあるが、値を追加する処理は未実装)

---

## 7. UI機能(実装済み)

- **HOW TOポップアップ**: どのシーンからでも開ける画面内オーバーレイ。現時点では仮のテキストのみを表示する。
- **OPTIONポップアップ**:
  - SOUND ON/OFFの切り替え(設定値の保存のみ)
  - 進行状況(`storyEnemyIndex`)のリセット(確認ダイアログ付き)
  - 図鑑ボタン(取得済みアイテムが1つ以上ある場合のみ表示。押すと「準備中です」というアラートが出る)
  - バトル画面から開いた場合のみ RETRY(直前のデッキ編成へ戻る)/ RETURN TO TITLE(ロゴシーンへ戻る)ボタンを表示する

---

未実装・予定の内容は [`TODO.md`](./TODO.md) を参照。
