# TODO(未実装・予定)

実装済みの内容は [`IMPLEMENTATION.md`](./IMPLEMENTATION.md) を参照。
ここに記載する内容は、コード内のコメントで「今後実装予定」「準備中」等と明記されている、または関数が定義済みでも呼び出されておらず機能していないことをコードから確認できたものに限る。

## サウンド

- ON/OFF設定(`state.soundOn`)は保存・復元されるが、実際の音声再生処理は未実装。
- 音声アセット用フォルダ(`assets/audio/bgm/`, `assets/audio/se/`)は用意済み。

## HOW TO(あそびかた)

- 現在はポップアップの表示機構のみ実装済みで、本文は仮テキスト。差し替え予定とコード内に明記されている。

## 隠しアイテム図鑑

- OPTION画面の図鑑ボタンを押すと「準備中です」というアラートが表示されるのみ。
- アイテムを取得済みリスト(`unlockedItems`)へ追加する処理自体が未実装(読み込み・表示判定のロジックはあるが、値を追加する経路がない)。

## 敵キャラクターの見た目

- `ENEMY_PRESETS` に5体分の敵情報は定義済みだが、専用グラフィック(`enemy_〇〇.PNG`)は全敵共通の1セットしか読み込まれない仕組みになっている。敵ごとに異なる見た目にするには、読み込み・参照ロジックの拡張が必要。

## 敵の行動パターン

- 各敵プリセットの `favoritePatterns` は空配列として定義されているのみで、実際の敵の行動(手札生成)には反映されていない。

## STORY MODEの敵進行

- 次の敵へ進める関数 `advanceToNextEnemy()` は定義済みだが、勝利時などどこからも呼び出されていない。
- 現状、STORY MODEでは常に `storyEnemyIndex` が指す同じ敵と対戦し続ける(OPTION画面から明示的にリセットしない限り変化しない)。

## テキスト・シナリオ

- プロローグ(`op_1〜4.PNG` に対応するテキスト)、ストーリーシーン(`story_1〜3.PNG` に対応するテキスト)は、いずれも仮テキストとしてコード内に直接記載されている。

## 画像アセット本体

- 敵専用グラフィック・背景・ロゴ・タイトルロゴ・カード画像・プロローグ/ストーリー画像は、いずれもフォールバック表示の仕組みは実装済みだが、実際の画像ファイル自体はまだ用意されていない。命名規則・配置場所は [`ASSETS.md`](./ASSETS.md) を参照。
